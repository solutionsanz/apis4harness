apiVersion: certificates.k8s.io/v1beta1
kind: CertificateSigningRequest
metadata:
  name: my-csr-name.my-namespace
spec:
  request: $(cat id_rsa.csr | base64 | tr -d '\n')
  usages:
  - digital signature
  - key encipherment
  - server auth